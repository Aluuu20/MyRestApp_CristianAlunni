<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Stato premuto -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#297289" /> <!-- Blu più scuro quando premuto -->
            <corners android:radius="12dp" />
        </shape>
    </item>

    <!-- Stato normale -->
    <item>
        <shape>
            <solid android:color="#B2FFFF" /> <!-- Blu principale -->
            <corners android:radius="12dp" />
        </shape>
    </item>
</selector>
